@model IEnumerable<InAndOut.Models.ExpensesType>



<div class="container p-3">
  <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">My Expenses Type</h2>
        </div>
        <div class="col-6 text-right">
             <a class="btn btn-primary" asp-area="" asp-controller="ExpensesType" asp-action="Create">Create New Expenses Type</a>
        </div>
   </div>
   <br />

    @if(Model.Count() > 0)
    {
       <table class="table table-bordered  table-striped" style="width:100%;">
          <thead>
            <tr>
              <th scope="col">My Expenses Type</th>
              <th scope="col">Changes</th>
            </tr>
          </thead>
          <tbody>
            @foreach(var type in Model)
            {
               <tr>
                <th width ="38%">@type.Name</th>
                <td width ="24%">
                    <div class="btn-group w-100%" role="group">
                     <a class="btn btn-primary px-3 m-1" asp-area="" asp-controller="ExpensesType" asp-action="Update" asp-route-Id="@type.Id">Update</a>
                     <a class="btn btn-danger px-3 m-1" asp-area="" asp-controller="ExpensesType" asp-action="Delete" asp-route-Id="@type.Id">Delete</a>
                    </div> 
                </td>
              </tr> 
            }     
          </tbody>
       </table>
    }
 
</div>

